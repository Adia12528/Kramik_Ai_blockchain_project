# Database Configuration
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=kramik_db
DB_PORT=5432

# JWT Secret
JWT_SECRET=your_jwt_secret_key_here

# Server Configuration
PORT=5000
NODE_ENV=development

# AI Chat Configuration - FREE API KEYS

# Groq API (Recommended - Very Fast & Free)
# Get your free key at: https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here

# Hugging Face API (Alternative - Free Tier Available)
# Get your free key at: https://huggingface.co/settings/tokens
HUGGINGFACE_API_KEY=your_huggingface_api_key_here

# Note: You only need ONE of the above API keys for the AI chat to work
# Groq is recommended for fastest response times
# If no API key is configured, the system will use a fallback mode with limited functionality
